# Runtime Configuration Index (v1)

This file is the runtime-configuration entry point for this repo.

Use the design-specific runtime contracts:

- **Design A (Microservices):** `docs/spec/runtime-config-design-a.md`
- **Design B (Monolith-per-node baseline scaffold):** `docs/spec/runtime-config-design-b.md`

Notes:
- Public API/proto/state semantics remain governed by the existing spec/proto authorities.
- Keep this index stable for discoverability; update linked design-specific files as runtime behavior evolves.

